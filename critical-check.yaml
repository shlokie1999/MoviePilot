id: git-directory-exposure
info:
  name: Git Directory Exposure
  author: yourname
  severity: critical

requests:
  - method: GET
    path:
      - "{{BaseURL}}/.git/HEAD"
    matchers:
      - type: word
        words:
          - "ref: refs/heads"
